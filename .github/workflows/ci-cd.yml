name: CI/CD

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - 'v*'

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Check, Build, and Package
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.12'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run checks
        run: npm run check

      - name: Package prebuilt installer artifact
        run: npm run pack:release

      - name: Smoke test packaged CLI
        run: |
          set -euo pipefail
          tmp_dir="$(mktemp -d)"
          tar -xzf artifacts/cnb-mr-helper.tar.gz -C "$tmp_dir"
          "$tmp_dir/cnb-mr-helper/dist/index.js" --version

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: cnb-mr-helper
          path: |
            artifacts/cnb-mr-helper.tar.gz
            artifacts/cnb-mr-helper.sha256
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download package artifact
        uses: actions/download-artifact@v4
        with:
          name: cnb-mr-helper
          path: artifacts

      - name: Publish release assets
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          if gh release view "$TAG_NAME" >/dev/null 2>&1; then
            gh release upload "$TAG_NAME" artifacts/cnb-mr-helper.tar.gz artifacts/cnb-mr-helper.sha256 --clobber
          else
            gh release create "$TAG_NAME" artifacts/cnb-mr-helper.tar.gz artifacts/cnb-mr-helper.sha256 \
              --title "$TAG_NAME" \
              --notes "Prebuilt CNB MR Helper package."
          fi
